package boj.problems

import java.io.BufferedReader
import java.util.Locale

object No15000 {
    fun solve(input: BufferedReader): String {
        return input.readLine().uppercase(Locale.getDefault())
    }
}
